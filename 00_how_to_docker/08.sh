docker run -it --rm alpine /bin/sh

# To exit container, use: exit
# To check if container properly exited, use: docker ps -a
# -it = allocate a pseudo-TTY (terminal)
# https://containertutorials.com/alpine/get_started.html
# --rm = deletes itself after
